{
  "name": "granola-cli",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "granola": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "test": "bun test",
    "build": "bun build src/index.ts --compile --outfile granola"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
